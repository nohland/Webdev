<p>
<a href="/">&laquo; Back</a>
</p>
<h1>99 Bottles of Beer Lyrics</h1>
<p><strong>This is an example of using a loop with a block in an ERB view. The lyrics are generated by a <em>Route</em> in the <code>app.rb</code> file, then rendered by the <em>View</em> in <code>views/bottles.erb</code></strong></p>
<% @lyrics.each do |line| %>
<p><%= line %></p>
<% end %>